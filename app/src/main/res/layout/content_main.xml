<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".View.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/main_bg_tech">
        
        <!-- 顶部选项卡 -->
        <android.support.design.widget.TabLayout
            android:id="@+id/fag_tabs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/tab_sidebar_bg"
            app:tabMode="scrollable"
            app:tabGravity="fill"
            app:tabTextColor="@color/tab_inactive_text"
            app:tabSelectedTextColor="@color/tab_active_text"
            app:tabIndicatorColor="@color/tab_indicator"
            app:tabIndicatorHeight="4dp"
            app:tabIndicatorFullWidth="true"
            app:tabPaddingStart="16dp"
            app:tabPaddingEnd="16dp"
            app:tabPaddingTop="12dp"
            app:tabPaddingBottom="12dp"
            app:tabTextAppearance="@style/TechTabStyle"/>
        
        <!-- 内容区域 -->
        <com.hik.netsdk.SimpleDemo.View.BusinessUI.NoSwipeViewPager
            android:id="@+id/fag_pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
    </LinearLayout>

</android.support.v4.widget.DrawerLayout>