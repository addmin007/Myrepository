# UniMOF v2 推理服务依赖
# 用于 Docker 镜像构建 / 本地 pip install -r requirements.txt

# ── Web 服务 ──────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # 支持 multipart/form-data（CIF 文件上传）

# ── 科学计算 ──────────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0

# ── 结构解析（可选但推荐）────────────────────────────────────────────────────
# pymatgen>=2024.1.1          # 完整 CIF 解析（含对称操作），较大（~200MB）
# ase>=3.22.0                 # 轻量级替代，约 30MB

# ── 可观测性 ──────────────────────────────────────────────────────────────────
# prometheus-fastapi-instrumentator>=6.1.0   # 暴露 /metrics 端点（可选）

# ── 注意 ──────────────────────────────────────────────────────────────────────
# torch / unicore 不在此列，在 Dockerfile 中单独安装：
#   torch 2.x 通过基础镜像 pytorch/pytorch:2.1.2-cuda12.1-cudnn8-runtime 提供
#   unicore 从 GitHub 源码安装
