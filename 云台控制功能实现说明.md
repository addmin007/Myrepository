# 云台控制功能实现说明

## 功能概述

已成功实现ClientDemo中的云台控制功能，包括方向控制、变焦控制和预置位控制。

## 实现的功能

### 1. 方向控制
- **向上移动** (`jButtonPTZUp`): 使用 `HCNetSDK.TILT_UP` 命令
- **向下移动** (`jButtonPTZDown`): 使用 `HCNetSDK.TILT_DOWN` 命令  
- **向左移动** (`jButtonPTZLeft`): 使用 `HCNetSDK.PAN_LEFT` 命令
- **向右移动** (`jButtonPTZRight`): 使用 `HCNetSDK.PAN_RIGHT` 命令

### 2. 变焦控制
- **放大** (`jButtonPTZZoomIn`): 使用 `HCNetSDK.ZOOM_IN` 命令
- **缩小** (`jButtonPTZZoomOut`): 使用 `HCNetSDK.ZOOM_OUT` 命令

### 3. 停止控制
- **停止** (`jButtonPTZStop`): 停止所有方向的移动，包括上下左右和变焦

### 4. 预置位控制
- **预置位1** (`jButtonPTZPreset1`): 转到预置位1
- **预置位2** (`jButtonPTZPreset2`): 转到预置位2  
- **预置位3** (`jButtonPTZPreset3`): 转到预置位3

## 技术实现

### 使用的API
所有云台控制功能都使用海康威视SDK的 `NET_DVR_PTZControl` 函数：

```java
boolean NET_DVR_PTZControl(int lRealHandle, int dwPTZCommand, int dwStop);
```

### 参数说明
- `lRealHandle`: 预览句柄，必须是有效的预览句柄
- `dwPTZCommand`: 云台控制命令（如TILT_UP、PAN_LEFT等）
- `dwStop`: 0表示开始移动，1表示停止移动

### 错误处理
每个云台控制方法都包含：
1. 检查预览句柄是否有效
2. 调用SDK函数执行控制命令
3. 检查返回值并输出相应的成功/失败信息
4. 如果失败，获取并显示错误码

## 使用条件

### 前置条件
1. **设备连接**: 必须先通过"注册"按钮连接到海康威视设备
2. **开始预览**: 必须先点击"预览"按钮开始视频预览
3. **有效句柄**: 需要有效的预览句柄 (`lPreviewHandle > 0`)

### 操作流程
1. 在IP地址、用户名、密码、端口字段输入设备信息
2. 点击"注册"按钮连接设备
3. 点击"预览"按钮开始视频预览
4. 使用云台控制按钮控制摄像头

## 界面布局

云台控制面板采用优化的布局设计：
- **方向控制**: 十字形布局，中心对称
- **变焦控制**: 水平排列的放大/缩小按钮
- **预置位控制**: 水平排列的三个预置位按钮
- **停止按钮**: 位于方向控制的中心位置

## 测试程序

创建了 `PTZControlTest.java` 测试程序，用于验证云台控制界面的布局和按钮响应：
- 模拟云台控制按钮的点击事件
- 在控制台输出相应的测试信息
- 验证界面布局的正确性

## 注意事项

1. **设备兼容性**: 云台控制功能需要设备支持PTZ功能
2. **权限要求**: 需要具有云台控制权限的用户账户
3. **网络连接**: 需要稳定的网络连接以确保控制命令及时传输
4. **预置位设置**: 预置位需要先在设备上设置才能使用

## 错误码说明

如果云台控制失败，可以通过 `NET_DVR_GetLastError()` 获取错误码：
- 常见错误码包括设备未连接、权限不足、命令不支持等
- 具体错误码含义请参考海康威视SDK文档

## 总结

云台控制功能已完全实现，包括：
- ✅ 方向控制（上下左右）
- ✅ 变焦控制（放大缩小）  
- ✅ 停止控制
- ✅ 预置位控制（3个预置位）
- ✅ 错误处理和状态反馈
- ✅ 优化的界面布局

所有功能都基于海康威视SDK的标准API实现，确保与设备的兼容性和稳定性。



